/**
 * Seed destinations into Appwrite from the local destination catalog.
 * This is a convenience alias for seed-appwrite.ts.
 * Run: npx tsx scripts/seed-destinations.ts
 */
export {};
console.log('Use: npx tsx scripts/seed-appwrite.ts');
console.log('(seed-destinations.ts is now an alias — the Appwrite seeder handles everything)');
import('./seed-appwrite');
